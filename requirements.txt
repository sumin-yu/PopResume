# Core
numpy
pandas
scipy
scikit-learn
xgboost
tqdm

# Plotting
matplotlib
seaborn

# Resume rendering / images
reportlab
PyMuPDF          # imported as `fitz`
Pillow           # imported as `PIL`

# Model APIs & local models
openai
google-genai     # imported as `google.genai`
requests
torch
torchvision
transformers

# Optional — only needed for specific paths
qwen-vl-utils    # imported as `qwen_vl_utils`; required only for Qwen2.5-VL scoring
# cupy-cuda12x   # optional GPU acceleration in 01-2 (PSID imputation); CPU path works without it
